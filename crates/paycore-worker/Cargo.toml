[package]
name = "paycore-worker"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
paycore = { path = "../.." }
async-trait = "0.1"
thiserror = "1"
time = { version = "0.3", features = ["serde"] }
uuid = { version = "1", features = ["serde"] }

[dev-dependencies]
pollster = "1"
